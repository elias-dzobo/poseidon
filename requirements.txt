# ── Core scientific stack ──────────────────────────────────────────────────────
numpy>=1.26
scipy>=1.12
pandas>=2.2

# ── Geospatial ─────────────────────────────────────────────────────────────────
rasterio>=1.3
pyproj>=3.6
shapely>=2.0
geopandas>=0.14
fiona>=1.9
gdal>=3.8               # system GDAL must be installed separately

# ── SAR preprocessing ──────────────────────────────────────────────────────────
# snappy (ESA SNAP Python interface) — install via SNAP installer, not pip
# Alternatively:
pyrosar>=0.23           # wraps SNAP CLI for SAR preprocessing

# ── Optical preprocessing ──────────────────────────────────────────────────────
s2cloudless>=1.7        # Sentinel-2 cloud masking
Pillow>=10.0

# ── Deep learning ──────────────────────────────────────────────────────────────
torch>=2.2
torchvision>=0.17

# ── VLM fine-tuning ────────────────────────────────────────────────────────────
transformers>=4.40
peft>=0.10              # LoRA / QLoRA
trl>=0.8                # SFTTrainer
bitsandbytes>=0.43      # 4-bit quantisation
accelerate>=0.29

# ── Data download ──────────────────────────────────────────────────────────────
requests>=2.31
tqdm>=4.66

# ── Hydrology (HAND computation) ───────────────────────────────────────────────
pysheds>=0.4

# ── Configuration ──────────────────────────────────────────────────────────────
pyyaml>=6.0
python-dotenv>=1.0

# ── Output / reporting ─────────────────────────────────────────────────────────
reportlab>=4.1          # PDF generation
matplotlib>=3.8

# ── Testing ────────────────────────────────────────────────────────────────────
pytest>=8.0
pytest-cov>=5.0
